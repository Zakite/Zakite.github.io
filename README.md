# zakite.github.io

Zak Jibrin&#39;s academic homepage: PhD candidate, Earth and Planetary Sciences, Rutgers.

Static site, no build step. `index.html`, `cv.html`, and `piece.html` are
self-contained; the CV and writing pages render live from Google Docs via
`gdoc.js`/`pieces.js`. Served verbatim by GitHub Pages (`.nojekyll`).
